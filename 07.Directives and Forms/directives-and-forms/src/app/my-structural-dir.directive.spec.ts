import { MyStructuralDirDirective } from './my-structural-dir.directive';

describe('MyStructuralDirDirective', () => {
  it('should create an instance', () => {
    const directive = new MyStructuralDirDirective();
    expect(directive).toBeTruthy();
  });
});
